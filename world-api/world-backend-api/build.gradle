apply from: '../../publishable.gradle'

dependencies {
    compile "org.heat:util:${ext.heatUtil}"
    compile "org.heat:shared-backend:${ext.heatShared}"
    compile "org.rocket:rocket-network:${ext.rocket}"
}
dependencies {
}



buildscript {
    dependencies {
        classpath "org.flywaydb:flyway-gradle-plugin:3.0"
        classpath 'org.postgresql:postgresql:9.3-1102-jdbc41'
    }
}

apply plugin: 'flyway'

flyway {
    url = 'jdbc:postgresql://localhost/heat_world_jiva'
    initVersion = 0
}